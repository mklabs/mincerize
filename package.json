{
  "name": "mincer-sourcemap",
  "version": "0.0.0",
  "description": "Takes a mincer compiled script as input stream, outputs the minified bundle with sourcemap",
  "main": "lib/mincer-sourcemap",
  "directories": {
    "bin": "bin"
  },
  "dependencies": {
    "debug": "~0.7.0",
    "commander": "~2.3.0",
    "mincer": "~1.1.3"
  }
}
